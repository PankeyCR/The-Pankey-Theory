# Set this only on the private computer or secure deployment host.
OPENAI_API_KEY=

# The server listens locally by default. Do not expose it publicly without authentication and rate limiting.
PORT=8787
